# v2(鋼鐵)→ v3(紡織)遷移清單 — 給 Henry 的 Claude Code session

> **建立**:2026-08-29 16:00 台北|**整理**:Claude(依 Henry 四項拍板)|**給誰**:Henry、Claude Code(執行)、Codex(審查)
> **前提**:repo `carbon-cred-demo` 於 commit `51ae362` 狀態(Phase 0–2 完成、Codex 修正已 commit、working tree 乾淨)。**遷移在 branch `v3-textile` 上做,`make test` 全綠 + `/codex:review` 後才合併。**
> **規格來源**:`docs/2026-08-26-專案架構決策.md`(2026-08-29-f 修訂)> `docs/demo情境設定與合成資料規格-v3.md` > 藍圖 v2 HTML(技術細節與 DoD)。本檔只是程式面的逐檔對照,**與規格衝突時以規格為準**。
> **四項拍板(Henry 2026-08-29)**:① 5 LE + 2 ECR + 2 workload、四分頁、六幕,鞋廠/幕 7/稽核層只做投影片;② 維持 Codex H2,M2 只給 `pcf_total`,`brand_allocation_share` 移入永不揭露;③ 幕 5 = 布廠付染整廠,A/B 差異 = 染整燃料,幕 6 撤 `pcf_dyeing` 為主線;④ 只換資料、欄位、劇本,技術層零改動。

---

## 0. 總覽:哪些檔要動、動多少

| 層 | 檔案 | 動作 | 量級 |
|---|---|---|---|
| 文件 | `docs/`:換入 spec v3、架構決策(-f)、本檔;`CLAUDE.md` 換 §8 全文 | 複製 / 覆蓋 | 5 分 |
| 別名(選項) | 全 repo alias 改名(Step 0) | sed + git mv | 15 分;做就一次做完,不做就只改顯示名 |
| 身分 | `scripts/presign-vlei.sh` | +1 actor `dye`、LEI 字首、虛構名、manifest 7 角色 | 20 分,重跑 `make presign` |
| 資料 | `data/seed.json` | 整檔換成 `docs/seed.v3.json`(隨本檔交付,已程式驗算) | 5 分 |
| 型別 | `shared/types.ts` | 三張憑證欄位常數 + tag 改名 | 40 分 |
| 憑證 | `server/creds/pcfUpstream.ts` → `tcCarbonUpstream.ts`;新增 `pcfDyeing.ts`;`pcfAggregate.ts` 三段公式 | 重寫 payload 組裝;公式改三輸入 | 1.5 小時 |
| 政策 | `server/policy/claims.ts`、`policies/*.cedar`、`server/policy/cedar.ts` | 機密名單、NEVER_DISCLOSABLE、M2 六欄;P3 常數 9500 | 30 分 |
| 委任 | `server/creds/mandate.ts` | purpose、aud、thresholds、counterparties、幣別 | 15 分 |
| 路由 | `server/routes/issue.ts`(+dyeing)、`aggregate.ts`(三輸入)、`demo.ts` | 小改 | 30 分 |
| 前端 | `App.tsx`、四個 tabs、`StackChart`、`CredCard` | 文案、標籤、三段圖、幕 4 按鈕 | 1–1.5 小時 |
| 測試 | `scripts/test.ts` | 寫死值與欄位名;新增 v3 檢查 | 1–1.5 小時 |
| 合計 | | | **約 6–7 小時**(不含 Phase 3) |

**不動**:`server/keys.ts`(只多載一把 `dye`)、`statuslist.ts`、`audit.ts`、`db.ts`/`schema.sql`、`discloseGateway.ts` 的驗證順序、`verifyPresentation.ts` 的檢查項、`presenter.ts` 的 NEVER_DISCLOSABLE 硬拒機制、`verify-chain.ts`、`verify-offline.ts`、`tamper.ts`、`AuditStrip`、`DenyStamp`、`Audit.tsx`。

---

## Step 0(選項,建議做):內部 alias 改名

現有程式以 `thepviet / hunggang / bruck / taiwanverify` 為 manifest key、鑰名、檔名與測試字串。紡織版不改也能跑(只換 `legal_name` 與介面文案),但 repo 會被評審/Codex 讀到「hunggang = 布廠」。**要改就在遷移第一步一次改完**,改完立刻 `make setup && make test` 確認仍全綠(v2 狀態),再進 Step 1。

```bash
git checkout -b v3-textile
# 1) 檔名
git mv server/creds/pcfUpstream.ts server/creds/tcCarbonUpstream.ts
git mv web/src/tabs/ThepViet.tsx web/src/tabs/Yarn.tsx
git mv web/src/tabs/BruckAgent.tsx web/src/tabs/BrandAgent.tsx
# 2) 內容(順序重要:先長字串後短字串)
FILES=$(git ls-files | grep -E '\.(ts|tsx|sh|json|md|cedar|sql|html)$' | grep -v -E '^(vendor/|docs/|package-lock)')
sed -i '' \
  -e 's/hunggang-workload/fab-workload/g' -e 's/bruck-workload/brand-workload/g' \
  -e 's/hunggang-gateway/fab-gateway/g'   -e 's/bruck-verifier/brand-verifier/g' \
  -e 's/hunggang_cfo/fab_cfo/g'           -e 's/hunggang-cfo/fab-cfo/g' \
  -e 's/bruck_cso/brand_cso/g'            -e 's/bruck-cso/brand-cso/g' \
  -e 's/taiwanverify/cb/g'                -e 's/TWVERIFY/CB/g' \
  -e 's/thepviet/yarn/g'  -e 's/THEPVIET/YARN/g' -e 's/ThepViet/Yarn/g' \
  -e 's/hunggang/fab/g'   -e 's/HUNGGANG/FAB/g' \
  -e 's/bruck/brand/g'    -e 's/BRUCK/BRAND/g' -e 's/Bruck/Brand/g' \
  -e 's/BruckAgent/BrandAgent/g' \
  $FILES
grep -rn -i -E 'thepviet|hunggang|bruck|taiwanverify' $FILES   # 應為 0
rm -rf .vlei data/vlei/* data/keys/* db/*.sqlite* && make setup && make test
```
注意:`984500HUNGGANG0001` 等 LEI 字首會被一起改成 `984500FAB0001`(長度不對),**Step 1 會整批換成 v3 字首**,不必在此修。`sed -i ''` 為 macOS 語法。

不做 Step 0 的替代:所有後續步驟把 `yarn/fab/dye/brand/cb` 讀成 `thepviet/hunggang/dye/bruck/taiwanverify`,並在 README 放一張對照表。

---

## Step 1:`scripts/presign-vlei.sh`(5 LE + 2 ECR)

```bash
echo "== [2/6] LEI =="
LEI_QVI=$(run lei make 984500QVISANDBOX00)
LEI_YARN=$(run lei make 984500SOIXANHVN001)
LEI_FAB=$(run lei make 984500CHENGFANG001)
LEI_DYE=$(run lei make 984500CAIHEDYE0001)
LEI_BRAND=$(run lei make 984500NORDLICHT001)
LEI_CB=$(run lei make 984500LOWLANDCB001)

echo "== [3/6] actors =="
run actor add --alias gleif --registry gleifRegistry --root
run actor add --alias qvi --registry qviRegistry --delegator gleif
for a in yarn fab dye brand cb; do run actor add --alias $a --registry ${a}Registry; done
run actor add --alias fab-cfo
run actor add --alias brand-cso

echo "== [4/6] issue(QVI → 5 LE → 2 ECR)=="
QVI_SAID=$(run issue --type qvi --issuer gleif --holder qvi --lei "$LEI_QVI" | said_of)
YARN_SAID=$(run issue --type le --issuer qvi --holder yarn  --lei "$LEI_YARN"  --auth "$QVI_SAID" | said_of)
FAB_SAID=$(run issue  --type le --issuer qvi --holder fab   --lei "$LEI_FAB"   --auth "$QVI_SAID" | said_of)
DYE_SAID=$(run issue  --type le --issuer qvi --holder dye   --lei "$LEI_DYE"   --auth "$QVI_SAID" | said_of)
BRAND_SAID=$(run issue --type le --issuer qvi --holder brand --lei "$LEI_BRAND" --auth "$QVI_SAID" | said_of)
CB_SAID=$(run issue   --type le --issuer qvi --holder cb    --lei "$LEI_CB"    --auth "$QVI_SAID" | said_of)
FAB_CFO_SAID=$(run issue --type ecr --issuer fab --holder fab-cfo --lei "$LEI_FAB" \
  --person "Lin Hsiu-Feng" --context-role "Finance Director" --auth "$FAB_SAID" | said_of)
BRAND_CSO_SAID=$(run issue --type ecr --issuer brand --holder brand-cso --lei "$LEI_BRAND" \
  --person "Anna Schäfer" --context-role "Chief Sustainability Officer" --auth "$BRAND_SAID" | said_of)
# [5/6] present ×7(含 dye.presentation.json);verify 兩張 ECR + dye LE
# [6/6] manifest roles:
#   yarn : 'Sợi Xanh Việt Co., Ltd.(越藍紗業)'      kind le
#   fab  : '誠紡實業股份有限公司'                     kind le
#   dye  : '彩合染整股份有限公司'                     kind le
#   brand: 'Nordlicht Sports AG'                     kind le
#   cb   : 'Lowland Certification B.V.'              kind le
#   fab_cfo  : '誠紡財務主管 Lin Hsiu-Feng'          kind ecr(alias fab-cfo)
#   brand_cso: 'Nordlicht 永續長 Anna Schäfer'      kind ecr(alias brand-cso)
echo "presign 完成:data/vlei/manifest.json + 7 份呈現包"
```
`server/keys.ts`:`loadSandboxKey()` 的允許 alias 集合加入 `dye`(其餘邏輯不動)。

---

## Step 2:`data/seed.json` ← `docs/seed.v3.json`

整檔以 `docs/seed.v3.json`(隨本檔交付於 `Research/files/v3-assets/seed.v3.json`)覆蓋。重點:`transaction.dyeing_service`(幕 5 交易,USD 3,420,付款人 fab、收款人 dye)、`emission_factor_table`(全部係數與出處)、`upstream_defaults`(紗;A=B 相同)、`dyeing_defaults` + `cases[*].heat_source / renewable_share`(A/B 差異唯一來源)、`aggregate_defaults`(損耗加成 1.0545、針織用電 2.4)、`status_list_idx`(固定 idx 表,取代分散在各 issuer 檔的常數)。`expected_*` 只給 test 用,issuer 不得讀取。

---

## Step 3:`shared/types.ts`

```ts
export type ClaimTag = 'public' | 'brand' | 'audit' | 'confidential' | 'unknown';   // customs→brand、customer→audit

// tc_carbon_upstream(YARN;spec v3 §4.2)
export const TC_UPSTREAM_PUBLIC_FIELDS = ['tcNo','tcStandard','tcProductStandardLabelGrade','tcProductCategoryCode','tcProductDetailCode',
  'tcCertifiedRawMaterialCountryOrArea','sellerTeId','buyerTeId',
  'tcShipmentInvoiceReferences_hash','unit_price_hash','energy_invoice_hash','recycler_name_hash','emission_factor_table_hash'] as const;
export const TC_UPSTREAM_BRAND_SD_FIELDS = ['tcProductRawMaterialCode','tcProductRawMaterialPercentage','tcProductCertifiedWeight',
  'tcShipmentDate','tcShipmentNo','inputTcNo','tcProductLastProcessorName','tcProductLastProcessorCountry','pcf_total','pcf_period','pcf_method'] as const;
export const TC_UPSTREAM_AUDIT_SD_FIELDS = ['pcf_direct','pcf_indirect','electricity_kwh_per_kg','pcf_factor_source'] as const;
export const TC_UPSTREAM_CONFIDENTIAL_FIELDS = ['invoice_refs','unit_price','energy_invoice','recycler_name'] as const;

// pcf_dyeing(DYE;§4.3)
export const PCF_DYEING_PUBLIC_FIELDS = ['process','facility_country','zdhc_incheck_level',
  'boiler_model_hash','fuel_contract_hash','chemical_inventory_hash','ppa_price_hash','emission_factor_table_hash'] as const;
export const PCF_DYEING_BRAND_SD_FIELDS = ['pcf_total','heat_source','renewable_share','pcf_period','pcf_method'] as const;
export const PCF_DYEING_AUDIT_SD_FIELDS = ['heat_mj_per_kg','electricity_kwh_per_kg','boiler_efficiency','pcf_direct','pcf_indirect','pcf_factor_source'] as const;
export const PCF_DYEING_CONFIDENTIAL_FIELDS = ['boiler_model','fuel_contract','chemical_inventory','ppa_price'] as const;

// pcf_aggregate(FAB;§4.4)
export const PCF_AGGREGATE_PUBLIC_FIELDS = ['product','hs6','origin','tcProductStandardLabelGrade','zdhc_incheck_level','precursor_refs',
  'plant_total_output_hash','capacity_utilization_hash','other_customers_hash','brand_allocation_share_hash','monthly_utility_commitments_hash'] as const;
export const PCF_AGGREGATE_BRAND_SD_FIELDS = ['pcf_total','pcf_period','pcf_method','tcProductRawMaterialPercentage','verification','quantity_kg'] as const; // = M2 六欄
export const PCF_AGGREGATE_AUDIT_SD_FIELDS = ['pcf_yarn','pcf_knitting','pcf_dyeing','yarn_loss_factor','knitting_electricity_kwh_per_kg','pcf_factor_source'] as const;
export const PCF_AGGREGATE_CONFIDENTIAL_FIELDS = ['plant_total_output','capacity_utilization','other_customers','brand_allocation_share','monthly_utility_commitments','utility_invoice_ref'] as const;

export type HeatSource = 'natural_gas' | 'coal';
export interface PrecursorRef { id: string; hash: string }
export type PcfCaseId = 'A' | 'B';            // 取代 PcfUpstreamCaseId / PcfAggregateCaseId(保留別名亦可)
// Payload interfaces:TcCarbonUpstreamPayload / PcfDyeingPayload / PcfAggregatePayload 依上表欄位;precursor_refs: PrecursorRef[](兩筆)
```

---

## Step 4:`server/creds/*`

**`tcCarbonUpstream.ts`**(原 `pcfUpstream.ts`):`vct = https://carbon-cred-demo.local/vct/tc_carbon_upstream`;A/B 讀同一組 `upstream_defaults`(不再依 case 變化,但仍為 A/B 各簽一張,idx 0/1);`pcf_indirect = round4(electricity_kwh_per_kg × grid_vn)`、`pcf_total = round4(pcf_direct + pcf_indirect)`;`emission_factor_table_hash = sha256({grid_vn, ...})`;四個機密項 → `*_hash`;disclosureFrame `_sd = [...BRAND_SD, ...AUDIT_SD]`;`issuerParty: 'yarn'`,`holderParty: 'fab'`。

**`pcfDyeing.ts`**(新;可直接複製 `tcCarbonUpstream.ts` 改):`vct = .../pcf_dyeing`;依 `cases[case].heat_source / renewable_share` + `dyeing_defaults` + `emission_factor_table` 計算——
```ts
export function computeDyeing(d, ef, heatSource: HeatSource, renewableShare: number) {
  const fuel = heatSource === 'coal' ? ef.coal_kg_per_mj : ef.natural_gas_kg_per_mj;
  const direct = round4(d.heat_mj_per_kg / ef.boiler_efficiency[heatSource] * fuel);
  const indirect = round4(d.electricity_kwh_per_kg * ef.grid_tw_kg_per_kwh * (1 - renewableShare));
  return { direct, indirect, total: round4(direct + indirect) };   // A: 3.0294/0.5972/3.6266;B: 5.747/0.8532/6.6002
}
```
idx:A=4、B=5;`reissue=1` 時用 idx 8 與新 `pcf_period`(幕 6);`issuerParty: 'dye'`,`holderParty: 'fab'`。

**`pcfAggregate.ts`**:`ensureUpstreamCredential()` 擴為 `ensureInputs(case)` → 取 `tc_carbon_upstream-<case>` 與 `pcf_dyeing-<case>`,**兩張消費前皆以 manifest 公鑰驗章**(驗不過丟 `UpstreamVerificationError`);公式改:
```ts
export function computeAggregateBreakdown(yarnTotal, dyeingTotal, c: { yarnLossFactor; knittingKwhPerKg; gridTw }) {
  const pcfYarn = round4(yarnTotal * c.yarnLossFactor);              // 3.1609
  const pcfKnitting = round4(c.knittingKwhPerKg * c.gridTw);          // 1.1376
  const pcfDyeing = round4(dyeingTotal);                              // 3.6266 / 6.6002
  return { pcfYarn, pcfKnitting, pcfDyeing, total: round4(pcfYarn + pcfKnitting + pcfDyeing) };   // 7.9251 / 10.8987
}
```
`precursor_refs = [{id: upstream.id, hash: sha256(upstream.sdJwt)}, {id: dyeing.id, hash: sha256(dyeing.sdJwt)}]`;機密五項 → `*_hash`(`monthly_utility_commitments_hash = sha256(JSON.stringify(array))`);回應多帶 `contract_carbon_max`(9.5)與 `breakdown` 供 StackChart。idx:A=2、B=3。

**`mandate.ts`**:`GATEWAY_AUD = 'fab-gateway'`、`RECEIPT_AUDIENCE = 'brand-verifier'`;M1 `extra`:`scope_tools` 不變、`max_amount: 50000`、`currency: 'USD'`、`allowed_counterparties: [manifest.dye.lei]`、`policy_thresholds: { carbon_max: 9.5, wallet_risk_max: 40, min_sources: 2 }`、`policy_version: 'pol-2026-08-v3'`;M2 `purpose: 'brand_scope3_supplier_reporting'`、`allowedClaims = M2_ALLOWED_CLAIMS`(六欄)。

**`invoice`**(幕 5,Phase 3 才用):issuer `dye`,`amount 3420 / currency USD / quantity_kg 2850 / payee_wallet` 讀 seed。

---

## Step 5:政策

**`server/policy/claims.ts`**
```ts
export const CONFIDENTIAL_CLAIM_NAMES = ['plant_total_output','capacity_utilization','other_customers','brand_allocation_share',
  'monthly_utility_commitments','utility_invoice_ref','chemical_inventory','fuel_contract','boiler_model','ppa_price','recycler_name','unit_price'] as const;
export const NEVER_DISCLOSABLE_CLAIMS = ['pcf_yarn','pcf_knitting','pcf_dyeing'] as const;   // H2:任兩項相減即還原第三項與布廠自家強度
export const M2_ALLOWED_CLAIMS = [...PCF_AGGREGATE_BRAND_SD_FIELDS];                          // pcf_total, pcf_period, pcf_method, tcProductRawMaterialPercentage, verification, quantity_kg
// tagForClaim:confidential 名單優先 → PCF_AGGREGATE_*_FIELDS 對應 'public' | 'brand' | 'audit' → unknown
// isSelectableDisclosure:tag === 'brand' || tag === 'audit'
```
**`policies/p2.cedar`** 註解列出上面 12 個機密名;**`p3.cedar`** 常數改 `context.carbon_total_g <= principal.mandate.carbon_max_g`(9500),註解「kgCO₂e/kg 布 × 1000」;`server/policy/cedar.ts` 的 mandate entity 屬性名同步(`carbon_max_kg` → `carbon_max_g`,值 = round(9.5 × 1000))。P1 不變。`make test` 的 `checkParsePolicySet` 照舊鎖住三條可解析。

---

## Step 6:路由與前端

- `routes/issue.ts`:`POST /api/issue/upstream`(YARN)+ 新 `POST /api/issue/dyeing?case=A|B[&reissue=1]`(DYE);`routes/aggregate.ts` 改呼叫 `ensureInputs`;`routes/demo.ts` 的案件/角色文案。
- `App.tsx` 分頁:`紗廠 Sợi Xanh Việt(簽發)` / `誠紡閘道(聚合 · Cedar)` / `Nordlicht 品牌 Agent(M2 · 查驗)` / `稽核與撤銷`;LEI 徽章讀 manifest。
- `Yarn.tsx`:表單欄位名就是 TE camelCase,旁註「Textile Exchange TC 欄位;`pcf_*` 為我方延伸——TC 本身無碳數據」;憑證卡三色:🟢 TC 公開欄 / 🟡 成分、認證重量、inputTcNo、pcf_* / 🔴 發票、單價、帳單、供應商名。
- `Gateway.tsx` + `StackChart.tsx`:三段 = 紗 / 織布 / 染整(B 案染整段紅、越過 9.5 門檻線,門檻讀 API 的 `contract_carbon_max`);鏈結圖示兩個參照指紋;文案「下游拿到的憑證裡沒有紗廠是誰、沒有染整廠帳單」。
- `BrandAgent.tsx`:M2 六個 chips(`pcf_total / pcf_period / pcf_method / tcProductRawMaterialPercentage / verification / quantity_kg`);presentation 六列旁註「全廠產量、帳單、紗廠名——連遮罩都沒有」;紅色按鈕改「**加碼索取 全廠產量 plant_total_output**」;驗證面板與 OFFLINE 不變。
- `CredCard.tsx`:層名 公開 / 品牌 / 稽核 / 僅指紋;單位 `kgCO₂e/kg`。

---

## Step 7:`scripts/test.ts`

寫死值替換:`'7318.15'` → `'6006.32'`;`1.5125` → `7.9251`;`'EAF'` → `'natural_gas'`(讀 `seed.cases.A.heat_source`);`specific_direct_embedded_emissions` / `production_route` → `pcf_total` / `heat_source`;`machine_energy` → `plant_total_output`;confidential 四項 → v3 名單;`hunggang/thepviet/bruck` 字串依 Step 0。
新增/改寫檢查:
1. manifest **7 個角色**(5 × kind=le + 2 × kind=ecr),`dye` 的 presentation 存在且 sandbox `verify --said` 通過。
2. `pcf_dyeing` A/B 由 `computeDyeing` 與獨立算式交叉驗證 = `seed.cases[*].expected_pcf_dyeing`;B ≠ A 且差異僅來自 heat_source/renewable_share。
3. `pcf_aggregate` A/B = `expected_pcf_total`;`precursor_refs` 恰兩筆且 hash 對得上入庫 sd_jwt;消費前驗章失敗路徑仍回 `CREDENTIAL_SIG_INVALID`。
4. H2:M2 presentation 內排放數字**恰一個**(`pcf_total`);索取 `pcf_yarn / pcf_knitting / pcf_dyeing` 任一 → `CLAIM_NOT_IN_MANDATE`,presenter 層亦硬拒;`brand_allocation_share` 不出現於任何 presentation 明文(只允許 `*_hash`)。
5. 幕 4:索取 `plant_total_output` → `POLICY_P2_CONFIDENTIAL` 且 deny 入鏈。
6. Cedar 單位:mandate entity `carbon_max_g == 9500`;A `carbon_total_g == 7925` 過、B `10899` 不過(Phase 3 P3 接上時鎖入)。
7. 一致性守門 grep(`server/ web/ scripts/ data/ policies/ CLAUDE.md`):不得出現 `鴻鋼|Thép Việt|Bruck|台驗|扣件|線材|CBAM|海關|EAF|BF-BOF|tCO2e/t|USDC|0x[0-9a-fA-F]{4}`;原有守門(human.key、workload.key、status_list_ref、encodedList、testnet 指示)照舊。

---

## Step 8:文件與 CLAUDE.md

- `docs/`:刪 `demo情境設定與合成資料規格-v2.md`,放入 `demo情境設定與合成資料規格-v3.md`;覆蓋 `2026-08-26-專案架構決策.md`(-f 版);放入本檔與 `seed.v3.json`;藍圖 v2 HTML 保留。
- `package.json` description → `跨組織碳足跡憑證選擇性揭露 demo(紡織:鞋用聚酯布情境)`。
- `CLAUDE.md` 全文換成 §8.1。

### 8.1 CLAUDE.md(v3 全文)

```markdown
# carbon-cred-demo — 專案硬規則(每個 phase 都遵守)

跨組織碳足跡憑證選擇性揭露 demo(**紡織:越南紗廠 → 台灣布廠 → 歐洲品牌;外包染整為 A/B 差異來源**)。文件優先序:
**docs/2026-08-26-專案架構決策.md(2026-08-29-f)> docs/demo情境設定與合成資料規格-v3.md > docs/製造貿易demo實作藍圖.html(技術細節與 DoD)**;程式面對照見 docs/2026-08-29-v2→v3遷移清單.md。
(藍圖之檔案路徑僅為示意,目錄一律以架構決策 §2 為準;Quan《260830 交接檔二》為敘事參考,與規格衝突處以 spec v3 §0.3 為準。)

## 範圍鐵則(優先於文件中任何相反描述)

- 憑證格式:@sd-jwt/core + @sd-jwt/sd-jwt-vc——**SD-JWT core = RFC 9901,SD-JWT VC = IETF Internet-Draft profile**。不得宣稱符合 W3C VC 2.0;UNTP DPP/PACT/**Textile Exchange TC 鍵名**僅作 claims 欄位對照,不宣稱 TE 認證。
- 應用層撤銷 = **Token Status List(draft-ietf-oauth-status-list-21)**:Status List Token 為 compact signed JWT,header.typ="statuslist+jwt",payload 含 sub、iat(建議含 exp、ttl)與 status_list = { bits: 1, lst: "<base64url(zlib 壓縮位元陣列)>" };credential 與 mandate 一律以 **status.status_list = { idx, uri }** 引用;正式檔案為 **data/status/mandates.jwt 與 data/status/credentials.jwt**,GET /status/* 必須回 compact signed JWT、Content-Type: application/statuslist+jwt;**驗證方必先驗 compact JWS 簽章,再解碼 payload.status_list.bits/lst**。vLEI 撤銷由 sandbox TEL 驗證。裸 JSON bit array 不得出現於正式流程。idx 固定表在 data/seed.json `status_list_idx`。
- 簽章金鑰(**5 LE + 2 ECR**,皆經 server/keys.ts 自 .vlei/state.json 匯出):YARN 紗廠 LE 鑰簽 tc_carbon_upstream;DYE 染整廠 LE 鑰簽 pcf_dyeing 與 invoice;FAB 布廠 LE 鑰簽 pcf_aggregate 與 Status List Token;CB 認證機構 LE 鑰簽 slcp_dcc/查證聲明;M1 與幕 5 人工放行用 **FAB 財務主管 ECR 鑰**;M2 用 **BRAND 永續長 ECR 鑰**。不建立 human.key。
- 另建兩把 app 產生的 workload 鑰:**fab-workload、brand-workload**。M1/M2 必須含 delegate_kid 綁定對應 workload 公鑰;跨組織 disclose request 必須由對應 workload 鑰簽章,閘道先驗簽再進 Cedar。
- 防重放:mandate 含 jti 與 mandate_nonce;每次 disclose request 帶新 request_nonce;(mandate_id, request_nonce) 加 UNIQUE;重複 → REPLAY_DETECTED;query_cap 扣次與 presentation、audit 寫入同一筆交易。
- seed 只有 A/B/C/Cp 四組;A/B 差異**只來自 pcf_dyeing 的 heat_source/renewable_share**;E 不是 fixture,是「撤銷 pcf_dyeing(A)後重跑 A」的狀態轉換(CREDENTIAL_REVOKED + 既有 Dossier DEPENDS_REVOKED → DYE 重簽 → 重聚合 → 重驗);撤 M1 為輔線(MANDATE_REVOKED)。
- Cedar 不得直接讀取 mandate 狀態:後端先驗 mandate 簽章、iss/aud/exp/jti、delegate_kid 對 request 簽章、Token Status List、request_nonce,再以 context.mandate_status_ok / delegate_key_ok / replay_ok 三個可信布林傳入;政策僅消費布林與 mandate 之資料欄位(allowed_claims 等)。
- **H2 算術洩漏防線(維持)**:跨組織 presentation 只能有一個排放數字(pcf_total);pcf_yarn / pcf_knitting / pcf_dyeing 為 NEVER_DISCLOSABLE(不進任何 allowed_claims,presenter 硬拒);三段熱點圖走 Tab 2 伺服端真值。brand_allocation_share、plant_total_output、capacity_utilization、other_customers、monthly_utility_commitments、原始帳單、化學品清冊、燃料合約、鍋爐型號、PPA 價格、回收粒供應商名、單價 一律 confidential(僅 hash)。M2 allowed_claims 恰為 pcf_total、pcf_period、pcf_method、tcProductRawMaterialPercentage、verification、quantity_kg 六欄。
- identity_vlei(法人/ECR)不掛應用層 Token Status List;其撤銷狀態一律以 vLEI TEL 參照、由 sandbox verify(child_process)查驗。
- 幕 5 = **布廠(FAB)付染整廠(DYE)**染整費:P3 檢查 DYE 身分、pcf_aggregate.pcf_total ≤ 9500 gCO₂e/kg(品牌合約 9.5 kgCO₂e/kg × 1000)、發票、收款帳戶風險雙來源;Dossier 由 fab-workload 簽;人簽 = 財務主管 ECR;產 **mock USD 電匯指令**(付款人 FAB、收款人 DYE、USD 3,420)→ 狀態 RELEASED。不得建立錢包、RPC 連線、testnet 位址或任何鏈上交易;收款帳戶識別碼為合成字串。
- **只做投影片、不寫程式**:鞋廠 SHOE 與其 M3 子集委任/幕 7/P4、稽核層 M4 與月度承諾值開啟、送出前一致性檢查(CONSISTENCY_FAILED)、slcp_dcc 加映(seed 一段即可)。
- out of scope:ZK/RISC Zero、鏈上/穩定幣、did:web、vckit、walt.id、OPA、APL。

## 硬規則

- docs/ 為唯一規格來源;禁止讀取 workspace 其他同名或舊版文件(含 v2 spec、鋼鐵版 seed)。
- 密碼學不可造假:簽章與驗章一律真實執行(@sd-jwt/*、Ed25519、sandbox verify);任何 mock 驗證視為 bug。
- 一方一鑰:取鑰只經 server/keys.ts;route 不得直接讀鑰檔或 .vlei/state.json。
- BRAND 端驗證(server/routes/verify 與 scripts/verify-offline.ts)只讀 token、manifest 公鑰、data/vlei/、data/status/;不得呼叫閘道 API 或讀他方 DB 資料。
- 所有 PERMIT/DENY/RELEASE/REPLAY_DETECTED 經 server/audit.ts 唯一入口,decisions 與 audit_chain 同一筆 transaction;DENY 與重放也入鏈。
- vendor/ 唯讀;.vlei/ 與 data/keys/ 永不進版控。
- 介面文案繁體中文;理由碼用 shared/codes.ts 的英文常數。
- 所有數值由程式以 data/seed.json 係數表計算,不得寫死結果;seed 的 expected_* 只供 make test。
- 每幕完成即執行 docs 藍圖對應的 DoD 檢查。
- **Phase 收尾流程(使用者 2026-08-29 指定)**:每完成一個大 phase,**push 之前必經 `/codex:review`**——提示使用者執行(或經使用者同意後代跑 codex 審查);逐項評估審查發現:需修正/優化者修完並使 `make test` 全綠(必要時加回歸鎖入 test.ts),不修者在回覆中寫明理由。**審查發現處理完畢,該 phase 才算結束、才可 push。**審查定案(影響後續 phase 的約束)一律追記到本檔「Codex 審查定案」章節。
  **遞補規則**:若 `/codex:review` 回報「無變更可審」或跑完零發現,改跑 **`/codex:adversarial-review`**,以其結果作為收尾依據。

## Phase 0 環境判定(2026-08-29 實測)

- **金鑰 Plan A 成立**:`.vlei/state.json` 之 `actors[alias].seed/verkey` 為 CESR qb64(code `A`/`D`,44 字元);解碼 = base64url("A"×1 + qb64[1:]) 去前 1 byte → 32B Ed25519 seed/公鑰。server/keys.ts 照此實作;v3 新增 `dye` 同法。
- **cedar-wasm 可用**:`@cedar-policy/cedar-wasm/nodejs` import 成功;policy.ts 純函式退路暫不需要,前端仍顯示 policies/*.cedar 原文。
- Status List Token 由 FAB LE 鑰簽署(閘道為 data/status/ 兩份清單的發布方);套件 @owf/token-status-list。
- blake3 已裝:sandbox 識別碼為 `E` 開頭。
- 常用指令:`make setup` / `make dev` / `make demo-reset` / `make test`。

## Codex 審查定案(2026-08-29;Phase 2.5/3 實作必須遵守)

- **Cedar 字串集合**:成員判斷用 `.contains()`,不得用 `in`。
- **Cedar 數值單位**:Cedar 不支援浮點、decimal 擴充無 `<=`——碳排一律以**整數 gCO₂e/kg 布**傳入政策(mandate 資料欄位維持 9.5 kgCO₂e/kg,後端 ×1000 轉 `carbon_total_g` / `carbon_max_g`)。make test 以 checkParsePolicySet 鎖住三條政策可解析。
- **稽核雜湊**:`payload_hash = sha256(event_type ‖ '\n' ‖ payload_json)`,`entry_hash = sha256(prev_hash ‖ payload_hash ‖ ts)`——event_type 必須在被簽章的序列化內;verify-chain.ts 照此公式驗證。
- **私鑰檔權限**:presign 以 `umask 077` 執行;`.vlei/` 700、`state.json` 600(make test 檢查);`data/vlei/*.json` 為公開材料,放寬 644。
- **稽核帶輪詢**:前端以最後收到的 seq 為游標(`/api/audit?after=<lastSeq>`)附加新事件,不得固定 after=0。
- **H2(Phase 2 總驗收)**:跨組織 presentation 內只能有一個排放數字;分項欄位 NEVER_DISCLOSABLE 由政策層與 presenter 層雙重拒絕。v3 對象為 pcf_yarn / pcf_knitting / pcf_dyeing。
- **F3/F4**:disclose 前驗被揭露憑證自身效期(CREDENTIAL_EXPIRED);presentation 由閘道簽章 receipt 綁定 presentation_hash + mandate_jti + request_nonce + aud + iat(RECEIPT_INVALID)。
```

---

## 9. Claude Code prompt(Phase 2.5 遷移;複製整段貼入 repo 內的新 session)

```
你是本專案的實作工程師。repo 目前在 commit 51ae362(Phase 0–2 完成,鋼鐵版 v2)。本次任務是 Phase 2.5:把場景自鋼鐵扣件遷移為紡織(鞋用聚酯布),技術層零改動,只換角色、欄位、數值、劇本。

先完整讀 docs/ 內四份文件再動工:docs/2026-08-26-專案架構決策.md(2026-08-29-f 修訂,最高優先)、docs/demo情境設定與合成資料規格-v3.md(欄位與數值唯一事實來源)、docs/2026-08-29-v2→v3遷移清單.md(逐檔對照,含 CLAUDE.md 全文與本 prompt)、docs/製造貿易demo實作藍圖.html(技術細節與 DoD);seed 檔為 docs/seed.v3.json。除 docs/ 外禁止讀取 workspace 中其他同名或舊版文件。衝突時:架構決策 > spec v3 > 遷移清單 > 藍圖。

範圍鐵則以遷移清單 §8.1 的 CLAUDE.md v3 全文為準(第一步就用它覆蓋 CLAUDE.md)。四項已拍板、不得更動:① 5 LE + 2 ECR + 2 workload、四分頁、六幕,鞋廠/幕 7/M3/P4/稽核層 M4/一致性檢查只做投影片;② 維持 Codex H2:M2 只給 pcf_total 一個排放數字,pcf_yarn/pcf_knitting/pcf_dyeing NEVER_DISCLOSABLE,brand_allocation_share 只留 hash;③ 幕 5 = 布廠 FAB 付染整廠 DYE,A/B 差異只來自 pcf_dyeing 的 heat_source/renewable_share,幕 6 撤 pcf_dyeing 為主線;④ 所有 pcf_* 由 seed 係數表計算,不寫死。

工作順序(在 branch v3-textile 上):
0. git checkout -b v3-textile;決定是否做遷移清單 Step 0(alias 改名):做就先做完並 make setup && make test 全綠(仍為 v2 行為)再繼續;不做就在 README 放對照表並把後續 yarn/fab/dye/brand/cb 讀成既有 alias。
1. CLAUDE.md 覆蓋為 v3 全文;package.json description 改紡織。
2. scripts/presign-vlei.sh 依遷移清單 Step 1(+dye、LEI 字首、虛構名、manifest 7 角色);server/keys.ts 允許 dye;rm -rf .vlei data/vlei/* data/keys/* db/*.sqlite*;make presign 確認 7 份呈現包、兩張 ECR 與 dye LE 皆 chain verified。
3. data/seed.json 整檔換成 docs/seed.v3.json(不得手改數值)。
4. shared/types.ts 依 Step 3;server/creds 依 Step 4(tcCarbonUpstream.ts、新 pcfDyeing.ts、pcfAggregate.ts 三段公式、mandate.ts 常數);路由依 Step 6。
5. server/policy/claims.ts、policies/*.cedar、server/policy/cedar.ts 依 Step 5(機密名單、NEVER_DISCLOSABLE、M2 六欄、9500)。
6. 前端依 Step 6(分頁名、Yarn.tsx 表單、StackChart 三段、BrandAgent 六 chips 與「加碼索取 全廠產量 plant_total_output」按鈕)。
7. scripts/test.ts 依 Step 7:替換寫死值、新增七組 v3 檢查(manifest 7 角色與 dye verify、computeDyeing 交叉驗證、三段聚合與 precursor_refs 兩筆、H2 三段不可揭露且 brand_allocation_share 不出現於明文、幕 4 plant_total_output → POLICY_P2_CONFIDENTIAL、Cedar 9500/7925/10899、一致性守門 grep)。
8. make setup 從乾淨環境一鍵成功;make test 全綠;make dev 走一次幕 1–4 截圖確認畫面文案已無鋼鐵字樣。

驗收標準(全過才算 Phase 2.5 完成):make setup 乾淨環境一鍵成功;make test 全綠(含上述七組新檢查);git grep -iE "鴻鋼|Thép Việt|Bruck|台驗|扣件|線材|CBAM|海關|EAF|BF-BOF|USDC" -- server web scripts data policies CLAUDE.md 為 0;git status 乾淨(.vlei/、data/keys/、db/*.sqlite* 未被追蹤)。完成後停下來輸出簡短報告(各步結果、Step 0 是否執行、遇到的問題),並依 CLAUDE.md 收尾流程提示我跑 /codex:review;審查處理完畢才合併回 main 並開始 Phase 3(幕 5+6,以 spec v3 §7 直接實作)。
```

---

## 10. Phase 3(幕 5/6)紡織版備忘(接在遷移之後)

- **幕 5 P3 管線**(`POST /api/agent/run?case=`):`identity_ok` = DYE 的 vLEI 鏈 verify + `pcf_dyeing` 簽章/效期/Status List;`carbon_total_g` = 該案 `pcf_aggregate.pcf_total × 1000`;`invoice_ok` = DYE 簽的 invoice(USD 3,420、quantity 2,850)驗章;`wallet_risk / risk_sources_confirming` 讀 `risk_signals`;`amount ≤ max_amount`。Dossier(JWS by fab-workload)payload 含 build_hash、四項結果、輸入憑證 hash(三張)。案 B 在第二列紅 `CARBON_OVER_THRESHOLD`,UI 只剩「轉人工/補件」且放行按鈕不渲染。
- **幕 5 人簽**(`POST /api/human-sign`):財務主管 ECR 鑰簽 release;產 `mock_payment_instruction { instruction_id, payer: fab, payee: dye, amount: 3420, currency: 'USD', rail: 'mock_wire' }`;狀態 RELEASED(不是 PAID)。
- **幕 6**:`make tamper N=42` → verify-chain FAIL → untamper;`make revoke LIST=credentials IDX=4`(撤 pcf_dyeing-A,重簽 credentials.jwt)→ 重跑 `agent/run?case=A` → `CREDENTIAL_REVOKED`;既有 Dossier 依 `precursor_refs` 標 `DEPENDS_REVOKED`(黃 badge「依據已撤銷,需重驗」)→ `POST /api/issue/dyeing?case=A&reissue=1`(idx 8,新 pcf_period 2026-06)→ `POST /api/aggregate?case=A` → 重跑 A 綠;輔線 `make revoke LIST=mandates IDX=0` → `MANDATE_REVOKED`。口白帶 vLEI TEL 連鎖(sandbox `revoke --said <DYE LE>` → 其憑證 verify 失敗,選配演)。
- 錄影只拍 A + B;C/C′ 口述;加映與鞋廠子集委任投影片。

---

## 附錄 A:seed.v3.json(整檔;已程式驗算 A 7.9251 / B 10.8987)

隨本檔交付於 `Research/files/v3-assets/seed.v3.json`——複製到 repo 為 `docs/seed.v3.json`,遷移時再複製為 `data/seed.json`。驗算方式:以係數表重算 `pcf_indirect / pcf_total(紗)`、`pcf_dyeing(A/B)`、`pcf_yarn / pcf_knitting / pcf_total(聚合)`,全部等於檔內 `expected_*`;Cedar 整數 A 7925 ≤ 9500、B 10899 > 9500。

## 附錄 B:對 Quan/Yulia 的回饋(遷移之外)

- 交接檔二的六 LE、M2 六欄、幕 5 付款方向、`brand_allocation_share` 問題已在 spec v3 §0.3 逐條覆寫,請以 spec v3 為準更新講稿;幕 7、稽核層、一致性檢查改為「已設計、未實作」投影片。
- 簡報 P4 四層對照與 P7 分鏡請依 spec v3 §7 六幕重排;數值一律引 spec v3 §7–8(A 7.93 / B 10.90 / 門檻 9.5);具名引述依 §2.8 逐條授權。
